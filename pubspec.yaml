name: golorry_customer_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3

  # Google Maps
  google_maps_flutter: ^2.6.1
  http: ^1.6.0

  # Fonts
  google_fonts: ^6.2.1

  # Persistence
  shared_preferences: ^2.3.2
  cloud_firestore: ^6.2.0
  geolocator: ^14.0.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  